# Test Frontend

This is only a demo frontend for local and devnet testing.

## Start

```bash
yarn --cwd apps/test-frontend start
```

Set `LOWKIE_API_BASE` when the backend runs on a different origin.